/**
 * 統合テスト用のセットアップファイル
 */

// グローバルタイムアウトの設定
jest.setTimeout(30000);

/**
 * 全テストスイート実行前の処理
 */
beforeAll(async () => {
  console.log('🧪 統合テスト環境を初期化中...');
});

/**
 * 全テストスイート実行後の処理
 */
afterAll(async () => {
  console.log('✅ 統合テスト完了');
});
